[package]
name = "compositor-anim"
description = "Hyprland's animation curves, its animation tree, and the values they move"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "compositor_anim"
path = "src/lib.rs"

[dependencies]
compositor-config = { path = "../config" }

[lints]
workspace = true
