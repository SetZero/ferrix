[package]
name = "compositor-layout"
description = "Hyprland's dwindle and master layouts, workspaces and dispatchers, as rectangles and ids"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
compositor-config = { path = "../config" }

[lints]
workspace = true
