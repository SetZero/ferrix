[package]
name = "compositor-config"
description = "Hyprland's hyprland.conf syntax, parsed into the compositor's configuration"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
compositor-regex = { path = "../regex" }

[lints]
workspace = true
