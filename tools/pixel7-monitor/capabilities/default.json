{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The one window: the app's own commands and its events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
