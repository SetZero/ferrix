[package]
name = "ferrix-blkserve"
description = "A ring-3 block driver's serve loop: submissions off the block ring into a virtio-blk device, completions back, as logic the glue runs"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
license.workspace = true
publish = false

[dependencies]
ferrix-blkring = { workspace = true }
ferrix-virtio = { workspace = true }
ferrix-virtio-blk = { workspace = true }

[lints]
workspace = true
