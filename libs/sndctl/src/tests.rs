//! Host tests: the refine against alsa-lib's calls, the stream driven by a
//! model program and device, the messages byte for byte, and the session.

extern crate std;

mod message;
mod pcm;
mod refine;
mod session;
