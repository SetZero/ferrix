[package]
name = "ferrix-renderctl"
description = "The render control protocol between the kernel's render core and a ring-3 GPU driver, whatever the GPU is"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
license.workspace = true
publish = false

[dependencies]
ferrix-native-abi = { workspace = true }

[lints]
workspace = true
