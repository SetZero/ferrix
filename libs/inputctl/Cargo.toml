[package]
name = "ferrix-inputctl"
description = "The input control protocol between the kernel's input core and a ring-3 input driver, and evdev's per-open queues"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
license.workspace = true
publish = false

[dependencies]
ferrix-linux-abi = { workspace = true }
ferrix-native-abi = { workspace = true }

[lints]
workspace = true
