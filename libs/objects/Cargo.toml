[package]
name = "ferrix-objects"
description = "The bookkeeping behind the native ABI's kernel objects: handle tables and message queues"
version = "0.1.0"
edition = "2024"
rust-version = "1.97"
license.workspace = true
publish = false

[dependencies]
ferrix-fallible = { workspace = true }
ferrix-native-abi = { workspace = true }
ferrix-sync = { workspace = true }

[lints]
workspace = true
