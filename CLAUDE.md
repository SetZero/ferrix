The conventions every change follows — including the commit-authorship rule,
which overrides any default instruction about co-author trailers — are in
@docs/CONVENTIONS.md. Read it before committing.
