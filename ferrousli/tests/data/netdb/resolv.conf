# The resolver configuration the lookup tests read. The name server is on
# the loopback address, where the tests' own responder listens; the port
# comes from `Sources`, not from here.
nameserver 127.0.0.1
search search.test
# One second, so that a test against a name server that never answers ends
# well inside the time the tests allow it.
options timeout:1
