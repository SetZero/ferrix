# Reading the boot log

The AArch64 boot log opens with nine lines that look like failures. They are
not ours, and this file is the evidence, so that the next person to read a boot
log does not spend an afternoon on them the way the first one did.

Everything below was checked by experiment, not inferred.

## The nine firmware lines

```
UEFI firmware (version edk2-stable202408-prebuilt.qemu.org built at ...)
ArmTrngLib could not be correctly initialized.
Error: Image at 0005FDB6000 start failed: 00000001
Error: Image at 0005FD6D000 start failed: Not Found
Error: Image at 0005FCBA000 start failed: Unsupported
Error: Image at 0005FC3F000 start failed: Not Found
Error: Image at 0005FB65000 start failed: Aborted
Tpm2SubmitCommand - Tcg2 - Not Found
Tpm2GetCapabilityPcrs fail!
```

**These are EDK2 starting its own DXE drivers, before any boot device is
considered.** QEMU's `virt` machine has no TPM and no SMCCC true random number
generator, so the drivers for them fail to start and say so. The `Error: Image
at ...` addresses are inside the firmware volume in flash, not in our image.

*How this was established:* boot the same firmware with **no disk attached at
all** —

```
qemu-system-aarch64 -machine virt -cpu cortex-a72 -m 512 -display none \
    -serial stdio -net none \
    -drive if=pflash,format=raw,readonly=on,file=edk2-aarch64-code.fd \
    -drive if=pflash,format=raw,file=build/aarch64/uefi-vars.fd
```

— and all nine appear, byte for byte, followed by the EFI shell. Nothing of
ours is involved.

They can be made to go away by supplying a TPM (`-tpmdev`) or by using a
firmware build without those drivers. Neither is worth doing: the messages are
correct, and a firmware that lied about having a TPM would be worse.

## `ConvertPages: failed to find range 140000000 - 14000BFFF`

This one *is* about our image, and it is still benign.

```
BdsDxe: loading Boot0001 ...
ConvertPages: failed to find range 140000000 - 14000BFFF
BdsDxe: starting Boot0001 ...
```

It appears between "loading" and "starting", which is `CoreLoadPeImage`. EDK2
first tries to honour a PE image's *preferred* load address before falling back
to loading it anywhere and applying relocations.

The loader's PE header says:

| Field | Value |
|---|---|
| `ImageBase` | `0x140000000` |
| `SizeOfImage` | `0xC000` (12 pages) |
| `ImageBase + SizeOfImage` | `0x14000C000` |
| `.reloc` | present, 196 bytes |
| `RELOCS_STRIPPED` | not set |

`0x140000000 – 0x14000BFFF` is exactly the range in the message. `0x140000000`
is `lld-link`'s default image base for a 64-bit PE, and QEMU's `virt` machine
puts RAM at `0x40000000`, so five gigabytes is not in the memory map — the
preferred-address allocation cannot succeed, EDK2 says so, and then does the
normal thing: allocate anywhere, apply the relocations, run it. The image is
relocatable, which is what makes the fallback correct rather than lucky.

*How this was established:* attach a disk with no `BOOTAA64.EFI` on it, so
firmware finds the filesystem but loads nothing from it. The message does not
appear. Then read the PE header and compare the numbers, above.

It could be silenced by passing `/base:0x40000000` to the linker, and it should
not be: that address is right for one QEMU machine and wrong for every real
one. A cosmetic message beats a fixed load address that is a lie on hardware.

## `UEFI Non-Block Boot Device`

x86-64 names its boot option after the disk; AArch64 does not:

```
x86-64:  Boot0001 "UEFI QEMU HARDDISK QM00001 " from PciRoot(0x0)/Pci(0x1F,0x2)/Sata(0x0,0xFFFF,0x0)
AArch64: Boot0001 "UEFI Non-Block Boot Device"  from VenHw(837DCA9E-E874-4D82-B29A-23FE0E23D1E2,...)
```

The description is generated by the UEFI boot manager from the device path. The
x86 path ends in a `Sata()` node, so it can name the drive from its model
string. The AArch64 path is a vendor-defined node — `837DCA9E-...` is EDK2's
virtio-MMIO transport — with no model to read, so the boot manager falls back to
a generic description.

**The block stack itself is healthy**, which is the part that would matter.
Asking the EFI shell what it sees:

```
Shell> map -r
FS0: Alias(s):F0:;BLK0:
    VenHw(837DCA9E-E874-4D82-B29A-23FE0E23D1E2,003E000A00000000)
```

A block device and a filesystem on it, which is why the loader boots at all.

Note there is no `HD(...)` partition node in that path: the image
`xtask` writes is a bare FAT32 filesystem with no partition table, and firmware
mounts the whole disk. That is legal and works on both architectures. Adding a
GPT with a proper EFI system partition would produce a conventional device path
and a nicer description, and is worth doing when the image grows a second
partition to put a root filesystem in — not before.

## The rule

None of the above is filtered out of the boot log, and none of it should be.
`xtask` shows the guest's serial output verbatim, because the first time a real
failure appears there, it will look exactly like these do — and the only way to
tell the difference is to have already worked out which lines are furniture.
