{
  "//": [
    "Statements of the certified item that x86-64's coverage suite does not",
    "reach, each with the reason no run on the measured machine can. Read by",
    "scripts/gen-coverage-justification.py, which files each line in the",
    "category named here instead of its file's, and fails when a line named",
    "is no longer in coverage-residual-x86_64.json (a test reaches it now, or",
    "it moved) or when `match` is not on the first line named. Line numbers",
    "are the tree's the residual was measured on, main 195a2e93 and this branch. Written 2026-09-26 for F-10."
  ],
  "arch": "x86_64",
  "arguments": [
    {
      "file": "arch/speculation.rs",
      "lines": "126",
      "category": "absent-hardware",
      "match": "Defences(self.0 | other.0)",
      "why": "`with`'s only rows in the image are in `Plan::for_processor`'s arms for a speculation control the processor offers -- enhanced, automatic or always-on IBRS, STIBP, SSBD, IBPB. QEMU's TCG emulates none of them (`TCG doesn't support requested feature: CPUID.07H:EDX.spec-ctrl`), and `x86_cpu` asks for them only under a hypervisor. The same arms run under KVM: gate 6 on the AMD reference host prints `speculation defences: ..., AutoIBRS, STIBP, SSBD, predictor barrier on switch, ...`. The unconditional defences are folded into a constant and carry no row."
    },
    {
      "file": "arch/speculation.rs",
      "lines": "162",
      "category": "other-architecture",
      "match": "f.write_str(\"none\")?;",
      "why": "`none` names an empty set of defences, and no x86-64 processor's set is empty in the certified build: `Plan::for_processor` starts every plan from the index clamps, the SWAPGS fence, the cleared entry registers and the RSB fill (`x86_64/speculation.rs` 166-169). Only a `--mitigations off` build records `Defences::NONE` here, and that is not the certified configuration. ARMv7-A's plan for a Cortex-A7, which needs no defence, is empty; the line is that architecture's."
    },
    {
      "file": "arch/x86_64/clock.rs",
      "lines": "138",
      "category": "defensive",
      "match": "if spins > 1_000_000 {",
      "why": "The loop's second pass: the HPET's main counter read the same value twice in a row. At the 10 MHz or more the HPET specification requires, one read through the device window takes longer than a tick, so a counter that is counting always differs on the next read. What it defends against is a timer block firmware left present but stopped, which it reports as an error after a million reads instead of dividing by zero elapsed time later."
    },
    {
      "file": "arch/x86_64/clock.rs",
      "lines": "149",
      "category": "absent-hardware",
      "match": "&& let Some(tsc_hz) = calibrate_tsc_against_hpet(",
      "why": "The TSC is measured against the HPET only on a processor whose CPUID says the TSC is invariant (`invariant_tsc`). QEMU's TCG refuses the bit (`TCG doesn't support requested feature: CPUID.80000007H:EDX.invtsc`); `x86_cpu` asks for it under KVM, where gate 6 prints `clock TSC, calibrated against the HPET at ... MHz`."
    },
    {
      "file": "arch/x86_64/clock.rs",
      "lines": "165",
      "category": "absent-hardware",
      "match": "let tsc_hz = calibrate_tsc_against_hpet(",
      "why": "A 32-bit HPET, capability bit 13 clear, on a processor without an invariant TSC. QEMU's HPET always reports a 64-bit main counter and has no property that narrows it; the AMD chipsets the module comment names are the hardware that takes this line."
    },
    {
      "file": "arch/x86_64/clock.rs",
      "lines": "192-193, 199-200, 205, 209, 211-212",
      "category": "absent-hardware",
      "match": "fn calibrate_tsc_against_hpet(",
      "why": "The whole of the measurement against the HPET, which only an invariant TSC (line 149) or a 32-bit HPET (line 165) calls; the measured TCG processor has neither. Gate 6 under KVM runs it on every boot, as its `calibrated against the HPET` line says. The PIT's measurement beside it is reached, by the suite's `boot-legacy` gate on a q35 with `hpet=off`."
    },
    {
      "file": "arch/x86_64/console.rs",
      "lines": "186",
      "category": "absent-hardware",
      "match": "for _ in 0..SPIN_LIMIT {",
      "why": "The row is the loop's second and later passes, for a transmit holding register still full when a byte is to be written. QEMU's 16550 passes each byte to its character device as it is written and reports the register empty again at once, so the first read of the line status always finds room. A real UART at 115200 baud is busy for 87 us a byte."
    },
    {
      "file": "arch/x86_64/console.rs",
      "lines": "213",
      "category": "absent-hardware",
      "match": "for _ in 0..DRAIN_LIMIT {",
      "why": "As line 186, for `drain`: the second pass waits for the shift register, and QEMU's port reports the transmitter idle on the first read. The function's own comment says so: harmless under QEMU, whose port sends instantly."
    },
    {
      "file": "arch/x86_64/console.rs",
      "lines": "227, 232",
      "category": "absent-hardware",
      "match": "pub(crate) fn read_byte() -> Option<u8> {",
      "why": "The prologue and epilogue of `read_byte`'s standalone copy, which runs only through the poll pointer `read_console_byte` hands `console::input::read_byte`: input stays polled when `console::input::init` finds no interrupt for the port, on an x86-64 machine whose MADT gives COM1's line no I/O APIC input. q35 routes COM1 through the I/O APIC, so input is interrupt-driven from before the first read, and the handler runs `read_byte` inlined into `take_console_byte`, which is reached."
    },
    {
      "file": "arch/x86_64/cpu.rs",
      "lines": "494, 500",
      "category": "defensive",
      "match": "let empty = [0_u8; 10];",
      "why": "The triple fault is `reset`'s last way, taken only after the FADT's reset register and ten pulses of the keyboard controller's reset line have each failed to reset the machine. On q35 the FADT names port 0xCF9, which resets it at once: the suite's `boot-reset` gate shows the loader starting again. Reaching this would take a machine that ignores both documented resets."
    },
    {
      "file": "arch/x86_64/mod.rs",
      "lines": "140",
      "category": "failure-path",
      "match": "}",
      "why": "The closing brace of `check_exception_entry` is only the early return of a `?`, when the NMI, breakpoint or saved-register check has failed; the passing path leaves by the tail jump to `trap::check::run` (objdump of the measured image: `pop %rbp; ret` under line 140, `pop %rbp; jmp trap::check::run` under 139). A failed check stops the boot with `STAGE3_TRAPS`."
    },
    {
      "file": "arch/x86_64/mod.rs",
      "lines": "1238, 1240",
      "category": "defensive",
      "match": "for _ in 0..1000 {",
      "why": "The keyboard controller's reset pulse, tried only when the FADT's reset register did not reset the machine. q35's register resets it at once (`boot-reset`), so the wait for the 8042's input buffer never starts. What it defends against is firmware that names a reset register that does nothing, which Linux's reboot order also allows for."
    },
    {
      "file": "arch/x86_64/mod.rs",
      "lines": "1301",
      "category": "failure-path",
      "match": "cpu::hlt();",
      "why": "`halt`'s loop runs on a processor that stops for good: after a panic, on the stop IPI a panic sends, or on a boot that could not build a console. A passing boot ends through `shutdown`, whose `debug_exit` ends QEMU before `halt` is entered, and the suite's one deliberate panic (`test-shell`'s `ferrix.onexit=panic`) exits QEMU from the panic report without its trace showing this instruction run."
    },
    {
      "file": "arch/x86_64/paranoid.rs",
      "lines": "122",
      "category": "failure-path",
      "match": "crate::trap::fatal(",
      "why": "An exception nested on its own interrupt stack, which the stack's occupancy count exists to catch and stop on (FX-9006): the entry clears DR7 so that a breakpoint on the handler's own path cannot nest, and the stage 3 check shows it does not."
    },
    {
      "file": "arch/x86_64/paranoid.rs",
      "lines": "134",
      "category": "failure-path",
      "match": "MACHINE_CHECK => crate::trap::fatal(",
      "why": "A machine check, a hardware error the kernel stops on. QEMU raises one only when a host tool injects it (`mce` in the monitor), which no gate does, and a run that took one would be a failing run."
    },
    {
      "file": "arch/x86_64/paranoid.rs",
      "lines": "135, 137",
      "category": "failure-path",
      "match": "vector => crate::trap::fatal(",
      "why": "Any vector but #DB, NMI and #MC reaching the paranoid entry: only the double fault's gate also names an IST stack here, and a double fault stops the kernel."
    },
    {
      "file": "arch/x86_64/paranoid.rs",
      "lines": "149",
      "category": "failure-path",
      "match": "crate::trap::fatal(",
      "why": "A #DB in the kernel that no armed hardware breakpoint raised -- a single step or a general detect, neither of which the kernel asks for -- which it stops on. The breakpoints the stage 3 check arms each set their DR6 bit."
    },
    {
      "file": "arch/x86_64/smp.rs",
      "lines": "56",
      "category": "absent-hardware",
      "match": "MadtEntry::LocalX2Apic(local) if local.is_enabled() => {",
      "why": "An x2APIC entry in the MADT, which firmware writes only for a processor whose APIC ID does not fit the eight bits of a local APIC entry: on QEMU, more than 255 processors with interrupt remapping in x2APIC mode. The suite boots four and one; q35's firmware describes each with a local APIC entry."
    },
    {
      "file": "arch/x86_64/smp.rs",
      "lines": "242",
      "category": "defensive",
      "match": "return Err(\"the trampoline does not fit in a page\");",
      "why": "The trampoline's length is fixed when the image is built, by three symbols around the assembly in this file, and is well under a page. The refusal defends against an edit that grows it past 4 KiB, which would otherwise be copied over whatever follows its frame below one mebibyte."
    },
    {
      "file": "arch/x86_64/speculation.rs",
      "lines": "185, 190, 326, 332, 334, 342",
      "category": "absent-hardware",
      "match": "plan.spec_ctrl |= SPEC_CTRL_STIBP;",
      "why": "STIBP and SSBD in IA32_SPEC_CTRL, AMD's automatic IBRS in EFER, and reading each back: planned and applied only on a processor that offers them. QEMU's TCG offers none (`TCG doesn't support requested feature: CPUID.07H:EDX.spec-ctrl [bit 26]`, `stibp`, `ssbd`, `auto-ibrs`). Gate 6's KVM boot on the AMD reference host applies all three and reads them back on every processor: `speculation defences read back on 4 processors`."
    },
    {
      "file": "arch/x86_64/speculation.rs",
      "lines": "340",
      "category": "absent-hardware",
      "match": "held &= unsafe { cpu::read_msr(AMD_VIRT_SPEC_CTRL) }",
      "why": "AMD's VIRT_SPEC_CTRL, used for SSBD only on a processor that offers it without SSBD in IA32_SPEC_CTRL: a guest of an older AMD host. Neither TCG nor the KVM reference host (which offers SSBD directly) presents that combination."
    },
    {
      "file": "arch/x86_64/trap.rs",
      "lines": "500, 524",
      "category": "failure-path",
      "match": "pub(crate) const fn vector_name(vector: u64) -> &'static str {",
      "why": "`vector_name`'s standalone copy, called by `report_trap` and by the paranoid entry's fatal arms, both of which run only when the kernel is stopping. `classify` carries its own inlined copy, which the stage 3 fault check reaches with a program's `hlt`."
    },
    {
      "file": "arch/x86_64/trap.rs",
      "lines": "598",
      "category": "credited-elsewhere",
      "match": "Trap::PageFault(fault) => (SIGSEGV, SEGV_MAPERR, fault.address),",
      "why": "Run by the stage 3 fault check's read of an unmapped address, which ends the program with SIGSEGV. The line's only statement row is in the copy of `fault_signal` that `dispatch` inlines for traps that are not page faults (in the measured image, inlined through `user_fault` into `dispatch` at trap.rs:102), where the arm cannot run. The copy `handle_page_fault` inlines (under trap.rs:460) computes the si_code as the fault's present bit plus one, one instruction under line 597's row, which is reached."
    },
    {
      "file": "arch/x86_64/trap.rs",
      "lines": "614, 620, 623-624, 639, 645",
      "category": "failure-path",
      "match": "pub(crate) fn report_trap(frame: &TrapFrame) {",
      "why": "The register dump the trap path prints when the kernel stops on an exception it cannot handle (`trap::report`); a passing run never prints it."
    },
    {
      "file": "trap.rs",
      "lines": "30",
      "category": "defensive",
      "match": "Interrupt(u32),",
      "why": "The Interrupt arm of `Trap`'s derived Debug. The only formatting of a `Trap` is the `signal ... ended by signal` line in `user_fault_as`, whose trap comes from `user_fault` or `handle_page_fault`; `dispatch` sends every Interrupt to the controller and never there. Reaching this would mean the dispatcher had treated a device interrupt as a program's fault."
    },
    {
      "file": "trap.rs",
      "lines": "135",
      "category": "failure-path",
      "match": "fatal(frame, why, &crate::panic::catalog::SYSTEM_CALL_TRAP);",
      "why": "The stop when the architecture cannot read a system call trap's registers. On x86-64 only `int $0x80` classifies as one, and its gate is not open to ring 3."
    },
    {
      "file": "trap.rs",
      "lines": "138, 143-144",
      "category": "failure-path",
      "match": "Trap::IllegalInstruction => fatal(",
      "why": "An invalid opcode or an unexpected exception taken in the kernel, which stops it. The same exceptions from a program are reached, by the stage 3 fault check, through the arm above these."
    },
    {
      "file": "trap.rs",
      "lines": "242",
      "category": "defensive",
      "match": "None => Outcome::Return(Errno::ENOSYS.as_return_value()),",
      "why": "A system call with no dispatcher registered. `main.rs` registers the item's dispatcher before anything can enter user mode, and the first registration stands, so in this item every call finds one. The arm is what the core answers when built without a dispatcher above it, rather than a panic in a trap vector."
    },
    {
      "file": "trap.rs",
      "lines": "345",
      "category": "failure-path",
      "match": "fatal(",
      "why": "A fault from user mode with no personality registered, which the kernel stops on; `main.rs` registers the Linux personality's return path before the first program."
    },
    {
      "file": "trap.rs",
      "lines": "363",
      "category": "failure-path",
      "match": "FaultOutcome::NoProcess => fatal(",
      "why": "A fault from user mode that the personality finds no process for, which the kernel stops on: every task that enters ring 3 is a process's thread."
    },
    {
      "file": "trap.rs",
      "lines": "400",
      "category": "defensive",
      "match": "return Err(None);",
      "why": "A fault from user mode on a task with no address space. `sched` gives a task made for a thread its process's space before it first runs, so this would be the kernel having entered ring 3 without recording who was running; it is reported as fatal rather than resolved."
    },
    {
      "file": "trap.rs",
      "lines": "478, 480, 483-486",
      "category": "failure-path",
      "match": "report(",
      "why": "The report of a page fault the kernel could not resolve -- one in the kernel outside the demand window, or a user one with no program to blame -- printed as the kernel stops."
    },
    {
      "file": "trap.rs",
      "lines": "500, 505, 515, 523-525, 527",
      "category": "failure-path",
      "match": "pub(crate) fn fatal(",
      "why": "`fatal` and `report`, the trap path's stop: the architecture's register dump, the abridged report for a second failure, and the panic's conclusion. Reached only when the kernel stops on a trap."
    },
    {
      "file": "smp.rs",
      "lines": "734, 738-739",
      "category": "failure-path",
      "match": "debug_assert!(",
      "why": "The failure arm of a `debug_assert!` and its message's arguments: a shootdown requested while holding a lock that disables preemption, which the assertion exists to stop on. It holds on every run of the suite."
    },
    {
      "file": "smp.rs",
      "lines": "760",
      "category": "failure-path",
      "match": "debug_assert!(",
      "why": "The failure arm of a `debug_assert!`: a shootdown that has to wait for other processors requested with interrupts masked, which no other processor could answer. It holds on every run."
    },
    {
      "file": "smp.rs",
      "lines": "801",
      "category": "failure-path",
      "match": "stood / 1_000_000",
      "why": "The millisecond count in the `fatal!` a processor raises when no shootdown turn started for the timeout while it waited, printed as the kernel stops."
    },
    {
      "file": "smp.rs",
      "lines": "857",
      "category": "failure-path",
      "match": "waited / 1_000_000",
      "why": "The millisecond count in the `fatal!` raised when a processor never answered a shootdown or a grace period, printed as the kernel stops."
    }
  ]
}
